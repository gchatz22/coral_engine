//! `jarvis_temporal` — stage 0.1 stub.
//!
//! This crate reserves the workspace path for the Temporal-hosted agent
//! workflow runtime that lands in stage 0.2 onward (see
//! `scratch/temporal_staged_plan.md` § 5). It is intentionally empty
//! today: no types, no deps, no behavior. The first real content is the
//! Temporal Rust SDK smoke in JAR2-? (stage 0.2).
