//! `node-run` — hand-driven smoke binary for the `jarvis_node` crate.
//!
//! In the bootstrap ticket this only prints a stub line so the binary
//! target is wired up. Later tickets replace `main` with the real loop
//! driver (config + JSONL trigger source).

fn main() {
    println!("node-run: jarvis_node bootstrap stub");
}
